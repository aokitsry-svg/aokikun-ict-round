# Codex実装指示 — 「あおき君」Phase 1

あなたは、このリポジトリの実装担当です。  
公開OSSの感染対策ラウンドアプリをベースに、独自ブランド版「あおき君」を完成させてください。

## 目的

元アプリとほぼ同じ機能・操作感を維持しながら、名称、ロゴ、キャラクター、配色、紹介情報を「あおき君」に置き換えることが目的です。

今回は商用サービス化ではなく、個人でもAIコーディング支援によって同程度のWebアプリを短期間で作れることを体験する試作です。大規模な再設計や過剰な機能追加は不要です。

## ベースリポジトリ

https://github.com/yukihiroyamaguchi569/ict-round

ベースリポジトリがまだ作業環境にない場合は、Forkまたはcloneした作業リポジトリを用意してください。元リポジトリを直接破壊的に変更しないでください。

## 最初に読むファイル

まず以下だけを読み、変更計画を短く提示してから実装してください。

- `package.json`
- `src/App.tsx`
- `src/types.ts`
- `src/checklistData.ts`
- `src/components/RoundStart.tsx`
- `src/components/MainScreen.tsx`
- `src/components/ReportPreview.tsx`
- `src/analytics.ts`
- `src/index.css`
- `index.html`
- `public/manifest.json` またはmanifest相当ファイル
- `public/sw.js`
- `LICENSE`
- `NOTICE`
- このパッケージの `01_phase1_spec.md`
- このリポジトリ直下の `AGENTS.md`

実装中は、必要な関連ファイルだけ追加で確認してください。

## 提供アセット

引き継ぎパッケージ内に以下があります。

- `assets/aokikun-logo.png`
- `assets/character-source.png`

実装時は、以下のように配置してください。実際の構成に合わせてパスは調整して構いません。

- `public/assets/brand/aokikun-logo.png`
- `docs/brand/character-source.png`

`character-source.png` は制作元資料です。通常の画面には写真のまま表示せず、承認済みロゴ内の清書キャラクターを使ってください。

## 実装タスク

### 1. ブランド変更

全画面と静的ページを「あおき君」へ変更してください。

- 「めぐる君」を「あおき君」に変更
- 元サービス固有のキャッチコピーを次に変更  
  `院内感染ラウンドを、もっとやさしく、たのしく。`
- HTML title、meta description、OGP、manifest、PWA表示名を変更
- 元ロゴ、元キャラクター、元人物写真を置換
- 元開発者の人物紹介と元サービスの問い合わせフォームを削除
- `/about/` は簡素な紹介ページとして残す
- Word報告書内に元ブランド名が表示される場合は変更
- PWAの名称は `あおき君`
- short_nameは `あおき君`

元の名称や人物名が、LICENSEまたはNOTICE以外の利用者向け画面に残らないようにしてください。

### 2. デザイン

- 白基調
- 主要色はコーラル／赤橙
- 補助色はソフトブルーとミントグリーン
- 元アプリの画面構成と操作の簡潔さを維持
- 開始画面に承認済みロゴを配置
- メイン画面ではロゴを小さく表示するか、文字ロゴに切り替える
- ロゴを小さくして説明文が読めなくなる場合は、キャラクター＋文字だけに見えるようCSSで表示領域を調整するか、ヘッダーではテキスト表記を使う
- タップ領域を狭くしない
- かわいさを優先して業務画面の可読性を下げない

### 3. 元機能の維持

以下を壊さないでください。

- ラウンド開始
- 担当者名と病棟名の入力
- 標準チェックリスト
- 9カテゴリ・22項目相当の初期構造
- A/B/C評価
- CSV/XLS/XLSX取り込み
- 項目に紐づく写真
- 一般写真
- 写真コメント
- 総評
- 箇条書き補助
- レポートプレビュー
- Word `.docx` 生成
- Web Share API対応環境での共有
- 端末内保存
- 保存済みラウンドの再開
- 保存済みラウンドの削除
- テーマ選択
- PWA
- オフライン用アセットキャッシュ

### 4. データ保存

Phase 1では保存方式を大きく変えません。

- 既存の `localStorage` を維持
- クラウド、API、データベースを追加しない
- 既存の保存キーに元ブランド固有名が含まれている場合は、必要に応じて新ブランド用キーへ変更
- 旧保存データ移行は不要
- 保存容量を超えた際の既存エラー処理を壊さない

### 5. 外部通信の削減

- Google Analyticsを完全に削除
- `src/analytics.ts` が不要になれば削除
- Analytics初期化・イベント送信のimportと呼び出しを削除
- Analytics用環境変数を削除
- 外部Google Fontsを使用している場合はシステムフォントへ置換
- 新しい外部APIを追加しない

完了後、コード検索で `google-analytics`、`gtag`、Analytics測定ID、外部フォントURLが残っていないことを確認してください。

### 6. 音声入力

- 総評の音声入力ボタンを非表示または削除
- 通常のテキスト入力と箇条書き補助は残す
- 音声入力にしか使われないコードや型が、変更によって完全に未使用になった場合のみ削除
- 関係のない部分はリファクタリングしない

### 7. Word出力

- Word出力を必ず維持
- ファイル名に「あおき君」またはラウンド情報を自然に含める
- 日本語ファイル名が不安定な環境を考慮し、既存方式が安定している場合は過剰に変更しない
- ロゴ挿入が簡単で安定する場合のみ、報告書上部へ小さく挿入
- ロゴ挿入がdocx生成を不安定にする場合は、文字見出し「あおき君 感染対策ラウンド報告書」でよい

### 8. PDF

Word実装完了後、低負荷で追加できる場合だけ対応してください。

実装条件:

- 新しい重量級PDFライブラリを追加しない
- レポートプレビューに `印刷・PDF保存` ボタンを追加
- `window.print()` と印刷用CSSを使用
- 印刷時はナビゲーション、操作ボタン、不要な背景を非表示
- A4縦を基本にする
- 写真や表が極端に分断されないよう、可能な範囲で `break-inside` を調整

安定しない場合は実装せず、最終報告で「PDFは見送った」と明記してください。

### 9. PWA

- manifestのname、short_name、descriptionを変更
- theme_colorとbackground_colorをブランド色へ変更
- アプリシェルのキャッシュ対象へ新しいブランドアセットを追加
- 古い元ブランドアセットをキャッシュ対象から外す
- Service Workerの更新時に古いキャッシュが残り続けないよう、キャッシュ名を更新
- 開発時のService Worker解除挙動を維持
- PWAアイコンは既存の仕組みを壊さず、可能な範囲で新ブランドへ置き換える
- 承認済み横長ロゴだけでは正方形アイコンに不向きな場合、無理な画像生成はせず、コーラル背景に白文字「あ」を配置した単純なSVGまたはPNGアイコンを作成してよい

### 10. 紹介ページ

`/about/` は、元ページをほぼ同じ構成で簡素化して構いません。

表示内容:

- あおき君
- 院内感染ラウンド支援アプリ
- チェックリスト、写真、Word報告書
- 端末内保存
- PWA対応
- 使い方4ステップ
- 「個人制作の試作であり、正式な医療情報システムではない」という注記
- アプリへ移動するボタン

削除する内容:

- 元開発者の氏名・肩書
- 元会社名
- 元問い合わせフォーム
- 元ユーザー会
- 元QRコード
- 実態のない問い合わせ先

### 11. 法的表示

- 既存の `LICENSE` を削除・変更しない
- 既存の `NOTICE` を保持する
- 必要に応じてNOTICEへ派生版の追記を行う
- READMEへ、元OSSをApache License 2.0に基づき改変した試作である旨を記載
- 元サービスとの公式な関係を示唆しない

### 12. ドキュメント

READMEを「あおき君」向けに更新してください。

最低限含める内容:

- アプリ概要
- 試作品であること
- 主な機能
- セットアップ
- 開発サーバー
- build/lint
- 端末内保存であること
- 未保存データとブラウザデータ消去の注意
- Word出力
- PDF対応の有無
- PWA
- ライセンスと派生元

## 実装原則

- 最小変更を優先
- 元アプリと同等なら十分
- 新しい状態管理ライブラリを追加しない
- 新しいUIライブラリを追加しない
- 新しいバックエンドを追加しない
- IndexedDBへ移行しない
- 関係のないリファクタリングをしない
- TypeScriptの型エラーを残さない
- 変更した箇所だけを整理する
- 不確実な部分は、元実装を維持する

## 検証

少なくとも次を実行してください。

```bash
npm install
npm run lint
npm run build
```

可能ならブラウザで以下を確認してください。

1. 新規ラウンド開始
2. A/B/C評価
3. 写真追加と削除
4. 総評入力
5. 保存
6. 再開
7. Word出力
8. CSVまたはXLSX取り込み
9. `/about/`
10. モバイル幅
11. PWA manifest
12. オフライン再読み込み

## 完了時の報告形式

以下の順で報告してください。

1. 実装した内容
2. 変更した主要ファイル
3. 検証結果
4. PDF機能を追加したか、見送ったか
5. 残っている既知の制約
6. 起動方法
7. スクリーンショットを確認すべき画面
8. 次の改善候補。ただし実装はしない

実装を開始してください。
